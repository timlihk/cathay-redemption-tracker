# Server
PORT=8080
BASE_URL=http://localhost:8080

# Email (SMTP)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_user
SMTP_PASS=your_password
SMTP_FROM="CX Monitor <noreply@example.com>"

# Playwright
# Set to 1 to run headful for debugging
PW_HEADFUL=0
# Optional: set to 1 to use an existing Chrome (if available)
PW_CHROMIUM_CHANNEL=

# App
CRON_SCHEDULE=0 * * * *
SECRET_KEY=please-change-me-32+chars

# Optional notifiers (webhooks)
DISCORD_WEBHOOK_URL=
SLACK_WEBHOOK_URL=
PUSHOVER_USER_KEY=
PUSHOVER_API_TOKEN=